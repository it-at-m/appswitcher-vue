name: appswitcher-vue custom

# Dependency Licensing Alerts
licensing:
  conditions:
    ids:
      - GPL-*
      - LGPL-*
      - AGPL-*

  warnings:
    # Warning is the dependency isn't known
    ids:
      - Other
      - NA
  ignores:
    ids:
      - pkg:npm/escape-string-regexp
      - pkg:npm/path-exists
    names:
      - pkg:npm/slash
      - pkg:npm/yocto-queue
